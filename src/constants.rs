pub const MAX_SUPPLY: u64 = 21_000_000; // Same as Bitcoin
pub const INITIAL_REWARD: u64 = 50; // Bitcoin started with 50 BTC per block
pub const HALVING_INTERVAL: u64 = 210_000; // Bitcoin halves every 210,000 blocks